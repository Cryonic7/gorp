# Research log

## Pass 1 (2026-09-17)
10 topics, docs/RESEARCH.md. Search service died partway; [unverified] flags left.

## Pass 2 — verification (2026-09-17)
All flags resolved. 88 citable URLs. Corrections: P4 L2=128B lines,
no QEMU pentium4 model, Lavapipe exists (doc corrected), 10-bit novelty confirmed.

## Pass 3 — CPU/GPU/OS support matrices (2026-09-17)
100 CPUs, 100 GPUs, 36 OSes. Stopped per >98%-non-novel rule after 6 batches / 17 searches.
Key: x86 family = one port; CHAR_BIT!=8 is a type-system wall; Linux = one syscall ABI.

## Pass 4 — deep dives (in progress)
See below.

### Pass 4 — Firmware deep dive (2026-09-17)
Topic: Multiboot 1/2 spec + coreboot payload model → docs/FIRMWARE_DEEP.md.
Batches run: 5 (MB1 header/magic, MB2 header/tags/machine state, spec-text fetch for MB1 info-structure layout, MB2 info tags + coreboot handoff, libpayload).
Novel findings: 24. Full MB1 header/flags field table (magic 0x1BADB002, checksum rule, bit-16 address fields, 8 KiB window); MB1 info-structure offsets incl. mmap entries and framebuffer fields; MB2 header constants (0xE85250D6, arch=0, 32 KiB window, 8-byte tags), EAX=0x36D76289 i386 machine state, tag type table; GRUB coexistence of both headers; coreboot CBFS+SELF ELF payload model, libpayload sysinfo, SeaBIOS-as-payload (Multiboot + option ROMs), GRUB2-as-payload; no Multiboot info struct for raw ELF payloads → coreboot tables reader or SeaBIOS/GRUB chain needed.
Stop reason: full batch (libpayload) returned only confirmatory detail, no new avenues; stop rule satisfied.
New sources: 13 (gnu.org MB1 spec, gnu.org MB2 spec text, coreboot Documentation/payloads.md ×2 mirrors, coreboot tutorial, osdev-notes MB2 tag-walking, multiboot2 header layout example ×2, multiboot.h from GRUB source, coreboot payload-API commit, libpayload MB-table commit, multiboot2-header asm example).

### Pass 4 — Shell grammar (2026-09-17)
Topic: POSIX sh + Windows cmd grammars → unified token model → docs/SHELL_GRAMMAR.md.
Batches run: 4 (POSIX expansion order + cmd for/delayed expansion; POSIX grammar tokens + cmd if/goto/call; cmd special chars/substring + POSIX quoting; cmd separators + POSIX lists/precedence).
Novel findings: 22. POSIX: token classes (WORD/ASSIGNMENT_WORD/IO_NUMBER), operator tokens (AND_IF/OR_IF/DLESS/etc), reserved-word recognition rules, metacharacter list, quoting table incl. $-quoting non-POSIX note, POSIX 4-stage expansion order, field-splitting/glob gating, pipeline/list precedence (&& || > ; &), async-& semantics, 126/127 exit codes. cmd: & vs && vs || semantics (ERRORLEVEL=0 = success), two-phase expansion (parse-time % vs execution-time !), for /f /r /d /l switches with tokens/delims/usebackq, %~ modifiers, substring %V:~s,l%, errorlevel>=N semantics, else-same-line rule, echo./echo(/@ quirks, ::-in-blocks hazard, >nul 2>&1 ordering, case-insensitivity, comma/semicolon arg separators, caret line-continuation.
Stop reason: batch 4 returned mostly confirmatory material on separators/precedence; corpus covers all SHELL_SPEC §2/§4 constructs. Stop rule satisfied.
New sources: 21 (NetBSD sh(1), bash expansions + parameter-expansion manuals, POSIX crash checklist, sh23 grammar rules, minishell tokens, bash Lists, IBM ksh/POSIX quoting, Debian bash(1), ss64 bash quoting, robvanderwoude variable expansion, ss64 for_f, MS Learn for + if, termux-cmd batch ref, welchbj windows.md, create-bat skill rules, ss64 conditional execution, O'Reilly Win2k cmds, ss64-syntax scribd mirror, spiceworks delayed-substring).

### Pass 4 — Avenue 5: Filesystem (FAT reference + GorpFS design)
- Topic: FAT12/16/32 on-disk essentials (BPB, FAT tables, dir entries, LFN, FSInfo, backup boot sector); GorpFS v0 native design; cell-model↔octet tension analysis.
- Batches run: 8. Novel findings per batch: B1 BPB/BPB-extended field tables; B2 FAT32 FSInfo/backup boot sector; B3 FAT12 12-bit packing pseudocode + EOC/bad/free markers; B4 LFN entry structure + short-name checksum; B5 Minix inode/zone/bitmap design; B6 ext2 superblock/block-group design; B7 PDP-10 byte pointers (arbitrary byte sizes — key precedent for the tension analysis); B8 littlefs metadata pairs + FAT timestamps; B9 FAT[0]/[1] dirty flags, geometry formulas; B10 exFAT (tangential, mostly confirmatory).
- Novel findings count: ~25 distinct facts/design inputs.
- Stop reason: >98%-non-novel rule — batch 10 returned only tangential/confirmatory material (exFAT), no new design-relevant facts for our goals.
- New sources: 24 web-verified URLs (see docs/FILESYSTEM.md Sources section).
- Output: docs/FILESYSTEM.md (FAT reference with field tables at exact offsets; cell-model tension analysis; GorpFS v0 superblock/inode/directory design with on-disk diagrams; 5-stage migration path).
- Key design decision recorded: GorpFS is byte-oriented on disk by construction; the cell model affects only the in-memory width_hint and the explicit cell<->octet marshalling layer (LSB-first canonical bit order, same convention as FAT12). 13-bit stays an in-memory stress case.

### Pass 4 — Linux i386 syscall ABI (2026-09-18)
Topic: Linux i386 `int $0x80` syscall ABI for the GorpOS Linux-compat shim → docs/SYSCALLS_I386.md.
Batches run: 7. B1 canonical table + register ABI + mmap2 page-units; B2 socketcall multiplexing + clone/signal + static-musl minimal call profile; B3 musl TLS startup + vDSO/SYSENTER staging + _llseek; B4 SYS_* socket constants + musl vDSO/int-0x80 fallback evidence + set_thread_area %gs contract; B5 ipc(117) subcalls + IPCCALL encoding + exit vs exit_group; B6 i386 clone arg-order reversal + pipe(42) single-pointer ABI; B7 pread64 64-bit alignment exception list + old/new select distinction.
Novel findings: ~19. Full `int $0x80` register ABI (EAX=nr; EBX..EBP args 1–6; EAX=-errno); SYSENTER/vDSO staged-out of v1 (musl i386 falls back to `int $0x80` when no AT_SYSINFO/vDSO is published); i386 `clone(120)` takes (flags, newsp, parent_tid, **tls, child_tid**) — last two args reversed vs x86-64; `mmap2(192)` offset in 4096-byte units; legacy `mmap(90)` takes packed-arg pointer; `_llseek(140)` split high/low offset, returns 0 and writes result through pointer; `socketcall(102)` EBX=subcall/ECX=array with all 20 subcalls 1–20 = SYS_SOCKET…SYS_SENDMMSG; `ipc(117)` EBX=`(version<<16)|op`, subcalls SEMOP=1…SHMCTL=24; `pipe(42)` takes one user pointer, kernel fills int[2], returns 0; `exit(1)`=current thread vs `exit_group(252)`=whole thread group (glibc `_exit` uses exit_group); i386 needs no special 64-bit register alignment for pread64/pwrite64 (ARM/MIPS/PPC32/Xtensa only); static musl startup needs set_thread_area/set_tid_address/rt_sigaction/rt_sigprocmask/brk-or-mmap2 — not just read/write/exit; recommended ordered first-~20 shim list in 4 stages (smoke binary → musl crt init → files).
Stop reason: batch 7 yielded only a partial novelty (alignment exception list); remaining gaps (i386 sigframe layout, stat64 field layout, old_select packed args, pread64 lo/hi register assignment) are minor and marked [unverified] in the doc; further searches returning re-confirmations.
New sources: ~20 web-verified URLs (full i386 register table, qemu syscall_32.tbl mirror, syscall(2) man ×2, wikibooks int-0x80, man-pages book PDF for mmap2, _llseek ×2, socketcall(2), uapi net.h, ipc.h blame mirror, clone(2) man PDF, fs/pipe.c, exit vs exit_group ×2, set_thread_area(2), signal(7), uros musl-defsysinfo commit, musl-wiki getting-started, osdev vDSO thread).
Output: docs/SYSCALLS_I386.md (§2 i386 ABI traps, §3 number+argument reference, §5 ordered first-20 shim list, §6 known gaps, §9 sources).
Most valuable finding: the i386 `clone` argument-order reversal (tls/child_tid swapped vs x86-64) — a single wrong register in the dispatcher would corrupt TLS for every threaded program.

### Pass 4 — VBE/VGA programming reference
- Topic: VBE 3.0 function/register definitions, ModeInfoBlock layout, banking, 4F0Ah PM interface, 4F15h DDC/EDID, Mode 13h and DAC ports, VGA text mode — doc-only reference for the GorpOS pre-GPU graphics stage.
- Batches run: 6. Novel findings per batch: B1 VBE 3.0 spec PDF + function map; B2 ModeInfoBlock full layout + PhysBasePtr/LinBytesPerScanLine rules; B3 banking granularity + WinSize vs WinGranularity; B4 4F0Ah PM interface limits (optional/restricted in VBE 3.0); B5 4F15h DDC/EDID call shapes (get-edid log, aero commit); B6 Mode 13h/DAC port programming + VGA text-mode cell/attribute format.
- Novel findings count: ~15 distinct register-level facts.
- Stop reason: all planned topics covered (4F00–4F0B, 4F15/DDC, ModeInfoBlock layout, DAC programming, bank-switch pattern, PM reachability options).
- New sources: 13 web-verified URLs (VBE 3.0 spec, VBE 2.0 mirror, VBE 3.0 mirror, Wikipedia VBE + Mode 13h + VGA text mode, faqs.org SuperVGA notes, petesqbsite VGA palette tutorial, OSDev forum ×3, tonymacx86 get-edid log, aero 4F15 commit).
- Output: docs/VBE_REF.md (~23 KB; 4F00–4F0B + 4F15 reference, 256-byte ModeInfoBlock offset table, Mode 13h/DAC programming, bank-switch pseudo-assembly, PM-entry strategy, recommended kernel subset). Post-write fixes applied: removed the unverifiable CoCalc/vbe.c citation, marked the QEMU-EDID claim [unverified], corrected the footer batch count to 6.
- Most valuable finding: VBE mode setup must happen in real mode before protected-mode entry; the kernel consumes a bootloader-provided framebuffer descriptor and uses LinBytesPerScanLine (not width-derived stride). VBE 3.0 protected-mode support is optional/restricted — not a kernel dependency.

### Pass 4 — Register-level 2D GPU research (Intel 8xx/9xx, ATI R100–R500, Matrox G200–G550, 3dfx Voodoo3)
- Topic: documentation availability for register-level 2D GPU programming on the GPU shortlist: PRMs/register specs, modesetting approach, 2D engine model, PCI IDs, ranked first-driver recommendation.
- Batches run: 13. Novel findings per batch: B1 Intel open IHD PRMs (newer gens) + xf86-video-intel chip coverage; B2 GMA900/950 2D engine specs (256-bit BLT engine, 400 MHz RAMDAC); B3 AMD R300 3D register guide + R500 docs + microcode release; B4 RAGE PRO Programmer's Guide (register-level 2D draw-op examples: DP_SRC/DST_Y_X/DST_HEIGHT_WIDTH); B5 R5xx Acceleration guide (2D_IDELCLEAN, CRTC flip via PM4) + x.org Radeon feature matrix (XAA/EXA DONE for R100–R500); B6 MGA-G200 full public spec at bitsavers (register-level DAC/CRTC/2D); B7 Voodoo3 Programming Guide r1.4a + Spec r1.0 + Data Book r1.0 + 3dfx open-source announcement; B8 Intel 845G/865G DID2 PCI IDs (8086:2562/2572) from GMCH datasheets; B9 Matrox PCI IDs (102B:0520/0521/0525) via DeviceHunt; B10 Intel 945G DID2 (8086:2772) + 865G BAR layout (MMADR/GMADR/IOBAR); B11 Banshee 2D Databook r1.01 (vendor 121a, device 0x3, register-level 2D) + Voodoo3 Avenger 121A:0005; B12 915G GMA900 TPS; B13 no-novel (driver-download/config pages only).
- Novel findings count: ~30 distinct facts.
- Stop reason: >98%-non-novel rule — batch 13 returned only driver-download and config-file pages, nothing new.
- New sources: 24 web-verified URLs (see docs/GPU_2D.md Sources sections; full list: IHD PRM PDF, intel(4) manpage, i915(7d), 82865G GMCH datasheet, 945 datasheet, D915GMH TPS, D945GNT TPS, x.org R5xx acceleration guide, Phoronix ×3, OpenBSD radeon(4), x.org RadeonFeature, r100.c cgit, RAGE PRO guide, MGA-G200 spec ×2 mirrors, DeviceHunt 102B page, Voodoo3 Programming Guide/Spec/Data Book, Banshee 2D Databook, Voodoo Graphics spec, The Register open-source story, computerhope Everest ID, Tom's Hardware i865G article).
- Output: docs/GPU_2D.md (~13 KB; per-family documentation verdict, modesetting approach, 2D engine, PCI IDs, ranked first-driver recommendation).
- Most valuable finding: 3dfx Voodoo3 has complete public register-level documentation including a dedicated 2D databook (Banshee 2D Databook r1.01) and is VGA-compatible — making it the lowest-risk first 2D-accelerated driver target, ranked ahead of Intel 865G/945G (datasheets public, but 8xx/9xx BLT/modeset are source-only).
- Unverified items (marked in doc): Intel 82915G DID2 8086:2582, Matrox G550 102B:2527, per-part ATI Radeon device IDs (vendor 1002h only confirmed).

### Pass 4 — Windows 3.1 NE executable format (2026-09-18)
Topic: NE ("New Executable") on-disk format for the GorpOS Phase-2 Win3.1 loader → docs/NE_FORMAT.md.
Batches run: 4. B1 NE header field map + e_lfanew detection + entry-bundle/relocation basics; B2 full entry-bundle encoding + resource table + name-table formats (via Microsoft KB Q65122 mirror); B3 ne_exetyp values + ne_flags full bits + in-memory segment entries + iterated segment data + OS/2-subset note + fixup-table placement; B4 ne_flagsothers/gangload (no novel material — stop signal).
Novel findings: ~20. Authoritative source located: Microsoft KB Q65122 "INF: Executable-File Header Format" (qb40/exe-format mirror). Detection: MZ → e_lfanew @0x3C → "NE" signature (0x4E45); full NE header offset map 00h–3Fh incl. ne_enttab/ne_segtab/ne_rsrctab/ne_restab/ne_modtab/ne_imptab (header-relative) vs ne_nrestab (file-relative); ne_flags bits (SINGLEDATA 0001h, MULTIPLEDATA 0002h, FIRSTDISC/NEBOUND 0800h, LINKERROR 2000h, NENOTMPSAFE 4000h, LIBRARY 8000h, OS/2 PM-windowing bits 0100h–0300h); ne_exetyp 00h–05h + 81h/82h (02h=Windows is the GorpOS target); 8-byte segment entries with ns_flags (TYPE_MASK CODE/DATA, MOVEABLE 0010h, PRELOAD 0040h, RELOCINFO 0100h, DISCARD F000h) and "0 = 64K" rules; resource table (rs_align, type blocks, 8-byte resources with MOVEABLE/PURE/PRELOAD, Pascal strings); resident/non-resident name tables (module name/description first, ordinal per name, zero-length terminator); module-reference table → imported-names table indirection; entry bundles (count byte, 0=end; 00h=unused; 01h–FEh fixed 3-byte entries; 0FFh moveable 6-byte entries with INT 3Fh thunk; ordinal 1 = first entry); fixup records appended after each RELOCINFO segment's data (source types LOBYTE/SEGMENT/FAR_ADDR/OFFSET, targets INTERNALREF/IMPORTORDINAL/IMPORTNAME/OSFIXUP, 0FFFFh-terminated source chains, ADDITIVE flag, OSFIXUP float-thunk types FIARQQ…FIWRQQ); iterated (RLE) segment data struct; heap/stack layout rule (SS==autodata && SP==0 → SP=top of autodata below heap); DLL init protocol (AX=module handle, far-return AX≠0); programs may only link against LIBRARY modules; OS/2's NE is a subset of the Windows format (2ine author).
Stop reason: batch 4 (ne_flagsothers bits, gangload detail) returned nothing novel — >98%-non-novel rule satisfied. Remaining bits (ne_flagsothers definitions, iterated-data walk rule, modref index base) marked [unverified] in the doc.
New sources: 7 web-verified URLs (qb40/exe-format = MS KB Q65122 authoritative spec; osFree NE wiki; Wikipedia New_Executable; Wikipedia format-comparison table; 2ine NE loader notes; DrDobbs OS/2 2.1 exe formats; archiveteam MS-DOS EXE e_lfanew notes). Full list in docs/NE_FORMAT.md §15.
Output: docs/NE_FORMAT.md (§3 header table, §6–§10 all tables + fixups, §11 loader responsibilities, §12 Windows-vs-OS/2, §13 GorpOS staging notes, §14 gaps).
Most valuable finding: the complete entry-bundle + fixup-chain encoding from Microsoft's own spec — the two pieces a loader cannot guess — plus the DLL init protocol and the SINGLE/MULTIPLEDATA instancing rules that drive DGROUP handling.

### Pass 4 — Avenue 5 batch-count correction (2026-09-18)
- Correction to the Avenue 5 entry above and to docs/FILESYSTEM.md's research footer (both previously stated "8 search batches"): the true Avenue 5 count was 11 batches at the time of writing. A final deliberately confirmatory batch was run on 2026-09-18 (FAT32 format layout, FSInfo/backup boot sector, FAT[0]/FAT[1] dirty flags, new fatgen103 mirror via scribd) and returned >98% already-covered material, so the strict stop rule is now formally satisfied at **12 batches**. The B1–B10 enumeration in the original entry is not a reliable batch record and is superseded by this note. docs/FILESYSTEM.md footer updated accordingly (append-only log entry left intact, corrected here).

### Pass 4 — Avenue 6: Tiny C compiler survey (2026-09-18)
- Topic: survey of small C compilers (tcc, cproc/QBE, lcc, chibicc, 8cc, cc65, pcc) for the GorpOS self-hosting path; explicit CHAR_BIT!=8 / packed-cell analysis; separate rankings for near-term i386 cross-compiler vs long-term cell-native codegen.
- Batches run: 9. B1 TinyCC (active forks z101/mingodad, i386/x86_64/ARM/AArch64/RISC-V, self-hosting, one-pass direct codegen, libtcc); B2 cproc/QBE (C99, QBE SSA IL, x86_64/aarch64/riscv64 only — no i386, byte-identical stage2/stage3 bootstrap, missing VLAs/volatile/TLS/long double/inline asm/PIC; FPGC B32P3 custom-CPU retarget precedent for the frontend→QBE→new-backend path); B3 lcc (Interface-struct retarget mechanism, literate-program book with full gen() source, historic x86 target files, ~75% smaller than gcc per authors' paper, besm6/erysdren forks incl. QVM/WASM experiments; license lineage restrictive [unverified]); B4 chibicc (AST→x86-64 asm, no IR, no optimizer, near-complete C11, "portability is not my goal"); B5 8cc + ELVM branch (word-addressed VM, sizeof(char)==sizeof(int)==sizeof(void*)==1, 24-bit words — key precedent for non-octet C) + pcc history (Johnson lineage); B6 cc65 (complete suite: compiler+ca65+ld65+archiver+simulator, 6502-only, active — organizational template for a self-hosting toolchain); B7 pcc current state (PortableCC ~16 targets, i386 listed, Debian 1.1.0 packaged, needs bison/flex to build; BSD lineage [unverified]); B8 CHAR_BIT!=8 precedents (TI TMS320C28x 16-bit char + __byte/__mov_byte intrinsics; isocpp P3477 — GCC/LLVM/MSVC all assume 8-bit bytes, POSIX mandates CHAR_BIT==8 since 2001, non-8-bit C is an incompatible dialect; DSP sub-word shift/mask synthesis); B9 saturation sweep — **SmallerC** (single-pass, 16/32-bit 80386+ NASM output, DOS/Windows/Linux/Mac OS X, MIPS backend, self-compiling, ships ucpp preprocessor + linker + gcc-like driver, already used to build hobby OS FYS OS; license [unverified]), selfie (educational, rejected), QBE target/scope confirmation.
- Novel findings count: ~30 distinct facts/assessments.
- Stop reason: judgment call on diminishing returns — every batch including B9 returned novel material (so the strict >98%-non-novel rule was NOT formally met), but the candidate set stabilized at 8 surveyed + 5 rejected (lacc, nwcc, cparser/libFirm, selfie, cc500/CIBIC) with 29 distinct sources banked; further batches would re-confirm rather than add candidates.
- New sources: 29 distinct web-verified URLs (see docs/COMPILERS.md Sources section).
- Output: docs/COMPILERS.md (25 KB; per-compiler table — architecture, IR/direct model, retarget mechanism, i386 status, license, activity; explicit CHAR_BIT!=8 analysis with DSP/ELVM/POSIX precedents; three rankings — near-term cross-compiler, long-term cell-native, teaching value; 4-stage plan).
- Most valuable finding: SmallerC — a tiny self-hosting i386-targeting C compiler shipping its own preprocessor, linker, and driver, already proven building a hobby OS — surfaced only in the final saturation sweep and is co-first-pick with TinyCC for the near-term cross-compiler; the choice between them is now license-and-taste, not capability. Long-term cell-native recommendation is cproc/QBE (smallest documented replaceable backend + proven custom-CPU retarget), with the explicit conclusion that cell-native C is a new data-model fork (10-bit char breaks int8_t/POSIX), not a backend port — reinforcing the FILESYSTEM.md decision that foreign binaries always see an 8-bit view.
- Unverified items (marked in doc): all licenses, LOC figures, reported w65c816 QBE backend.
